<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <Fallback>

      <!-- JIKA ADA HALANGAN -->
      <Sequence>
        <CheckObstacle/>
        <Rotate/>
      </Sequence>

      <!-- JIKA AMAN -->
      <MoveForward/>

    </Fallback>
  </BehaviorTree>
</root>
